defmodule Snitch.Data.Schema.PropertyTest do
  use Elixir.Case, async: true
  use Snitch.DataCase

  alias Snitch.Data.Schema.Property, as: PropertySchema
  import Snitch.Factory

  describe "create changeset" do
    test "is valid with correct params" do
    end

    test "is invalid with wrong params" do
    end
  end

  describe "update changeset" do
    test "is valid with correct params" do
    end

    test "is invalid with wrong params" do
    end
  end
end
